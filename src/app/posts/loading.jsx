import React from 'react';

const Loading = () => {
    return (
        <div>
            <h1><span className="loading loading-spinner text-primary"></span> Loading...</h1>
        </div>
    );
};

export default Loading;